name: Build site for GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-publish-root:
    if: ${{ !contains(github.event.head_commit.message, '[skip pages-root]') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install

      - name: Generate JSON data
        run: npm run generate:data

      - name: Check data
        run: npm run check:data

      - name: Build Vite app
        run: npm run build

      - name: Publish dist to repository root
        run: |
          rm -rf assets
          cp -R dist/* .
          touch .nojekyll
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html assets .nojekyll
          if git diff --cached --quiet; then
            echo "No Pages root changes to publish."
          else
            git commit -m "Publish built GitHub Pages site [skip pages-root]"
            git push
          fi
